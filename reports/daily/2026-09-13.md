# Daily Review — 2026-09-13

**Status: read-only review. No strategy, prompt, risk-parameter, or code changes were made or are recommended here.**

## Scope

Full UTC day, all three profiles (`baseline`, `aggressive`, `conservative`), each with a complete set of
records: 6 `experiment-` decision cycles (00/04/08/12/16/20Z) and 24 `risk-` checks (00Z–23Z) — **18 decision
cycles + 72 risk checks = 90 records total, no gaps in any profile's slot sequence.** Matching
`logs/workflow/*` (90 entries) and `logs/errors/*` (15 entries) confirmed. No `trades/baseline.csv` or
`trades/conservative.csv` rows fall on 2026-09-13 (no `aggressive.csv` exists at all — that profile has never
filled an order).

## Daily PnL

**Baseline** (00:00:05.805Z → 23:00:02.927Z), one open BTC-USDT position (qty ≈1.294e-5, from the
2026-09-11T08Z buy) carried through the whole day, no new fills:
- NAV at window start: **9.9991** (`experiment-baseline-2026-09-13T00Z`, `portfolio_before.last_equity` 9.99905).
- NAV at window end: **9.9918** (`risk-baseline-2026-09-13T23Z`).
- Change: **−0.0073 USDT (−0.073%)** — pure mark-to-market drift on the small BTC position; no fills, so
  realized PnL was flat (constant −0.0010, the cumulative fee from the 09-11 buy) and the swing is entirely
  `unrealized_pnl` (+0.0001 at 00:00Z → −0.0072 at 23:00Z).
- Intraday range: low NAV 9.9913 at 11:00Z, high 10.0000 at 19:00Z/21:00Z. Peak equity unchanged at 10.024899
  (carried from before 09-13). Max intraday drawdown from peak ≈0.34% (well under the 10% `max_drawdown` halt
  threshold); `halted: false` throughout.

**Aggressive** and **conservative** (both hold 100% cash, no position, no fills since profile init on
2026-09-12T20:33Z): NAV flat at **10.0000** for every one of their 30 records on 2026-09-13. No PnL, no
drawdown, no daily loss.

## Benchmark comparison

`benchmark_vs_strategy` in each profile's `state/<profile>/experiment.json` only holds the *current* (now
2026-09-14 ~12:xx) snapshot — there is no archived end-of-2026-09-13 value. To compare on the correct window,
`trading_bot.benchmarks.update()` was replayed deterministically (same code, same inputs: each cycle's `market`
prices and `mom_10` indicator) over every decision log from each profile's inception through its last 09-13
cycle. `btc_buy_hold`/`equal_weight` use fixed unit holdings so this reproduces the stored value exactly;
`momentum_baseline` rebalances every 4h off `mom_10`, so this is a full, exact replay, not an estimate.

Window: last cycle before 09-13 (~21:19–21:20Z on 09-12) → last cycle of 09-13 (~20:00–20:10Z):

| Series | Baseline start→end | Δ | Aggressive start→end | Δ | Conservative start→end | Δ |
|---|---|---|---|---|---|---|
| **Strategy NAV** | 9.9977 → 9.9993 | **+0.016%** | 10.0000 → 10.0000 | **0.000%** | 10.0000 → 10.0000 | **0.000%** |
| btc_buy_hold | 10.0491 → 10.0518 | +0.027% | 10.0139 → 10.0125 | −0.014% | 10.0141 → 10.0134 | −0.007% |
| equal_weight | 10.2007 → 10.1362 | −0.632% | 10.0131 → 9.9537 | −0.593% | 10.0185 → 9.9601 | −0.583% |
| momentum_baseline | 10.0000 → 9.9942 | −0.058% | 10.0000 → 9.9963 | −0.037% | 10.0000 → 9.9658 | −0.342% |

Baseline (slightly net long BTC) beat `equal_weight` and `momentum_baseline` and trailed `btc_buy_hold`
fractionally. Aggressive and conservative (100% cash, zero exposure) trivially beat all three benchmarks simply
because every benchmark had a negative day — this says nothing about either profile's strategy, since neither
has taken a position yet. One day is not evidence of anything; noted as observed.

## Trades summary

**0 fills, all three profiles, 2026-09-13.** No symbols, no sides, no fees to report.

## Notable Kimi decisions

Of **18** decision cycles across all profiles, only **3** got a live Kimi response — one per profile, all at the
day's last (20:00Z) slot:

- `experiment-baseline-2026-09-13T20Z` — 1 candidate considered: BTC-USDT, confidence 0.40, HOLD (`target_allocation` 0.1, already held).
- `experiment-aggressive-2026-09-13T20Z` — 3 candidates: BTC-USDT (0.55), ETH-USDT (0.50), SOL-USDT (0.45), all HOLD, `target_allocation` 0.
- `experiment-conservative-2026-09-13T20Z` — 5 candidates: BNB/BTC/ETH-USDT (0.30 each), SOL/XRP-USDT (0.25 each), all HOLD, `target_allocation` 0.

Every live call returned only HOLD assessments (no BUY/SELL candidate), so there was nothing for the adapter to
size. The other **15 cycles (83%)** got no model output at all — see Technical failures.

## Confidence calibration

Not meaningfully assessable: the only confidence values recorded today (0.25–0.55, all HOLD, no directional
call) never resulted in a trade, so there is no outcome to calibrate them against.

## Risk-engine interventions

**None.** Scanned all 18 decision logs' `adapter.considered` (all `result: "noop"`, none `resized`/`rejected`)
and `risk_result` (`status: "held"` throughout), plus all 72 `risk-` records' `risk_status`/`risk_exits` (all
`"held"`, all `risk_exits: []`). No stop-loss, drawdown, or daily-loss exit fired on any profile, any hour.
`halted: false` in every single record.

## Possible overtrading

None. `daily_orders` stayed at 0 in every record, every profile, all day (cap is 24). No repeat activity in any
symbol — nothing traded at all.

## Contradictory theses

None. No BUY/SELL candidate was proposed anywhere today (the three live calls all returned HOLD-only
assessments), so there is nothing to compare across cycles or profiles.

## Technical failures

**15 of 18 decision cycles (83%) failed to get a live model response**, all three profiles, all with the
identical error:

| Slot | baseline | aggressive | conservative |
|---|---|---|---|
| 00Z | TimeoutError | TimeoutError | TimeoutError |
| 04Z | TimeoutError | TimeoutError | TimeoutError |
| 08Z | TimeoutError | TimeoutError | TimeoutError |
| 12Z | TimeoutError | TimeoutError | TimeoutError |
| 16Z | TimeoutError | TimeoutError | TimeoutError |
| 20Z | **ok (live)** | **ok (live)** | **ok (live)** |

In every failed case `logs/workflow/...steps.model = "error_hold_fallback"` with the other steps
(`load_state`/`market`/`indicators`/`risk_gateway`) all `"ok"` — the fail-safe worked as designed (forced HOLD,
RiskGateway still ran, no crash, no missed commit, no state corruption).

`CHANGELOG.md`'s `infra — 2026-09-13` entry says the Kimi/NIM call timeout was raised 40s → 100s today,
specifically in response to the 2026-09-12 review's finding of 8/9 cycles timing out. **That fix does not appear
to have been in effect for most of 2026-09-13**: the 00Z/04Z/08Z/12Z/16Z cycles across all three profiles still
failed with the same `TimeoutError` message the old 40s deadline produced, and only the 20:00Z cycle — the last
of the day, for all three profiles simultaneously — succeeded. This pattern (clean cutover exactly at 20:00Z,
same slot, all tracks) is consistent with the fix being deployed sometime between the 16:00Z and 20:00Z cycles,
but this review cannot confirm an exact deploy timestamp: the repo's git history has been rewritten by the
automation's own commits (a `git pull --ff-only` at the start of this review found local `main` diverged from
`origin/main` with a forced update), so ordinary commit history for the code change itself is not available to
check against. Flagged for Stanley's attention below.

No missing runs: all `experiment-` slots (00/04/08/12/16/20Z) and all `risk-` slots (00Z–23Z) present for all
three profiles. `policy_hash` internally consistent per profile across every 09-13 record (baseline
`8dcd8044...`, aggressive `e38cadc1...`, conservative `b3cd4a38...`).

## Anomalies requiring manual inspection

- **⚠ The 2026-09-13 timeout fix (40s→100s) does not appear to have taken effect until the day's last cycle.**
  15 of 18 decision cycles today still failed with the exact `TimeoutError` message the CHANGELOG says was
  fixed today, across all three profiles, only clearing at the 20:00Z slot. If the fix actually deployed late in
  the day, this is expected and resolving; if it was live from the start of the day and cycles are still timing
  out at 100s, the underlying NIM latency issue flagged on 2026-09-11/09-12 is worse than the fix accounted for.
  Worth Stanley confirming the actual deploy time against server/deployment logs (not visible from this repo's
  history, which has been rewritten by automated commits).
- **Repo git history integrity note (process, not experiment data):** at the start of this review, local `main`
  had diverged from `origin/main` (`origin/main` had been force-updated, leaving ~50 prior commits
  unreachable from the tracked branch). This reviewer reset local `main` to `origin/main` (working tree was
  clean, nothing was lost locally) to read the latest state; it's mentioned here only because it's why exact
  commit timing for the CHANGELOG-documented timeout fix can't be verified from git log. `state/experiment.json`
  content (policy_hash, universe, cadence, experiment_version) matches `CHANGELOG.md`'s `v1.1` description in
  all three profiles — no evidence of a strategy-condition change today, unrelated to the history-rewrite
  behavior itself.
- Zero live trading activity to inspect otherwise: no fills, no risk exits, no resized/rejected proposals, no
  drawdown/daily-loss proximity on any profile today.
