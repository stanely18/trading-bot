# Daily Review — 2026-09-12

**Status: read-only review. No strategy, prompt, risk-parameter, or code changes were made or are recommended here.**

## Scope note — mid-day split into three profiles

For most of this UTC day the experiment ran as a single track (`experiment-2026-09-12T{00,04,08,12,16,20}Z` +
`risk-2026-09-12T{00..20}Z`), continuing the `v1.1` run that started 2026-09-11T02:33:47Z. At
**2026-09-12T20:33:29Z** (commit `0a687ea`, "Add multi-profile risk experiment: aggressive (5x leverage) +
conservative", authored by Stanley, not by this reviewer) the single track was renamed **`baseline`** and two new
profiles, **`aggressive`** (5x isolated-margin leverage) and **`conservative`** (half of baseline's order/position/
loss/drawdown caps), were initialized with a fresh 10.0 USDT ledger each. All three now run on independent state
under `state/<profile>/` and `trades/<profile>.csv`, sharing one git branch.

Records reviewed for `recorded_utc` in `2026-09-12`:
- 6 single-track `experiment-` cycles (00/04/08/12/16/20Z — all present, none missing) + 3 post-split
  `experiment-{aggressive,baseline,conservative}-21Z` cycles (9 decision cycles total).
- 21 single-track `risk-` checks (00Z–20Z, consecutive) + 6 post-split `risk-{aggressive,baseline,conservative}-{22,23}Z`
  checks (27 risk checks total). **No `21Z` risk check exists for any track** — see Technical failures.
- Matching `logs/workflow/*` and `logs/errors/*` entries, and `trades/baseline.csv` (no `aggressive.csv`/
  `conservative.csv` exist — zero fills on either new profile).

Because `aggressive` and `conservative` only existed for the last ~3.5h of the day (from 20:33Z), their per-day
figures below cover that partial window only, not a full 24h.

## Daily PnL

**Baseline** (full day, 00:00Z → 23:00:02.852Z):
- NAV at window start (`experiment-2026-09-12T00Z`, `portfolio_before`): **9.9974 USDT**.
- NAV at window end (`risk-baseline-2026-09-12T23Z`): **9.9985 USDT**.
- Realized PnL for the day: **0.0000** (the constant −0.0010 shown in every record's `pnl.realized_pnl` is the
  cumulative fee from the single 2026-09-11T08Z BTC-USDT buy; no new fills today).
- Unrealized PnL: −0.0006 at 00:00Z → −0.0005 at 23:00Z (open BTC-USDT position, qty ≈1.294e-5, essentially flat
  mark-to-market).
- Total PnL for the day: **+0.0001 USDT (+0.001% of NAV)** — flat, within noise.
- Peak equity for the day: 10.0249 (carried over from 2026-09-11); day low was 9.9973 (20:00:02.910Z). Max
  intraday drawdown from peak ≈0.276%, far under the 10% `max_drawdown` halt threshold; no proximity to the 2%
  `daily_loss` threshold. `halted: false` throughout.

**Aggressive** (partial day, 21:19:47Z → 23:05:02.678Z, ~1h45m):
- NAV at init: 10.0000 USDT. NAV at 23:05Z: **10.0000 USDT**. No PnL, no positions, no orders.

**Conservative** (partial day, 21:20:14Z → 23:10:02.451Z, ~1h50m):
- NAV at init: 10.0000 USDT. NAV at 23:10Z: **10.0000 USDT**. No PnL, no positions, no orders.

## Benchmark comparison

`benchmark_vs_strategy` in each profile's `state/<profile>/experiment.json` only refreshes on `experiment-` (4h)
cycles, not on hourly `risk-` checks (by design — risk checks do no benchmark rebalance), so the most recent
in-window snapshot is each profile's last 2026-09-12 decision cycle.

**Baseline** — 00:00:02.737Z snapshot → 21:19:03.964Z snapshot (last decision cycle of the day):

| Series | NAV @ 00Z | NAV @ 21Z | Change |
|---|---|---|---|
| **Strategy (Kimi + RiskGateway)** | 9.9984 | 9.9977 | **−0.0070%** |
| btc_buy_hold | 10.0427 | 10.0356 | −0.0707% |
| equal_weight | 10.1870 | 10.1845 | −0.0245% |
| momentum_baseline | 10.1148 | 10.0780 | −0.3637% |

The broader universe drifted down slightly through the day; the strategy — still ~90% cash with one small BTC
position — moved even less, so it outperformed (was least negative of) all three benchmarks on this window. One
day is not evidence of anything, noted as observed per scope.

**Aggressive** and **conservative** — both profiles' single 21Z snapshot shows `strategy_nav`, `btc_buy_hold`,
`equal_weight`, and `momentum_baseline` all at **10.0000** — too little elapsed time / price movement since
init (~45–50 min before the snapshot) for any benchmark or the strategy to have diverged at 4-decimal precision.
No comparison is meaningful yet for these two profiles.

## Trades summary

**0 fills** for any profile on 2026-09-12. `trades/baseline.csv` still shows only the single 2026-09-11T08:00:05Z
BTC-USDT BUY from the prior day; no `aggressive.csv` or `conservative.csv` file exists (consistent with zero fills
on either new profile).

## Notable Kimi decisions

Of **9 decision cycles**, only **1** got a live Kimi response:

- **`experiment-aggressive-2026-09-12T21Z`** (21:19:47Z, `chatcmpl-622ea2fd...`, latency 23.8s) —
  `market_regime: neutral`, `portfolio_view: neutral`, **zero candidates** → HOLD. This is a genuine model
  assessment (not a fallback), so it's the day's only real Kimi "decision," and it was to do nothing.

The other 8 cycles (all 6 single-track/baseline slots, plus `baseline-21Z` and `conservative-21Z`) got no live
model output at all — see Technical failures. In every case the adapter/RiskGateway correctly defaulted to HOLD
with `considered: []`, `chosen: null`.

## Confidence calibration

Not applicable — no candidate was proposed by Kimi at any point on 2026-09-12 (the one successful call returned
an empty candidate list). No confidence values exist to calibrate against outcomes.

## Risk-engine interventions

**None.** Across all 9 decision cycles' `adapter`/`risk_result` and all 27 `risk-` checks' `risk_status`/
`risk_exits`:
- 0 resized proposals, 0 rejected proposals (nothing was ever proposed to resize or reject).
- 0 stop-loss / drawdown / daily-loss / liquidation exits in `logs/risk/`.
- `halted: false` throughout every record, all three profiles.

## Possible overtrading

None. `daily_orders` stayed at 0 in every record, every profile, all day — no cycle came anywhere near the
`max_daily_orders` cap of 24, and there was no repeat activity in any symbol (nothing traded at all).

## Contradictory theses

None. No symbol had any candidate proposed (the one live model call returned zero candidates), so there is
nothing to compare across cycles or profiles.

## Technical failures

**8 of 9 decision cycles (89%) failed to get a live model response** on 2026-09-12:

| run_id | error |
|---|---|
| experiment-2026-09-12T00Z | `TimeoutError: The read operation timed out` |
| experiment-2026-09-12T04Z | `TimeoutError: The read operation timed out` |
| experiment-2026-09-12T08Z | `TimeoutError: The read operation timed out` |
| experiment-2026-09-12T12Z | `TimeoutError: The read operation timed out` |
| experiment-2026-09-12T16Z | `TimeoutError: The read operation timed out` |
| experiment-2026-09-12T20Z | `TimeoutError: The read operation timed out` |
| experiment-baseline-2026-09-12T21Z | `TimeoutError: The read operation timed out` |
| experiment-conservative-2026-09-12T21Z | `HTTP 429 from nvidia_nim: "Too Many Requests"` |

All six single-track timeouts and the `baseline-21Z` timeout show `duration_ms` ≈ 42,000 (a consistent ~42s
before the client gives up); the `conservative-21Z` 429 failed fast (~2s). In every case
`logs/workflow/...steps.model = "error_hold_fallback"` but `result: "ok"` — the fail-safe (timeout/error → forced
HOLD, RiskGateway still runs) worked as designed with zero operational impact (no crash, no state corruption, no
missed commit). This continues a pattern already flagged in the 2026-09-11 review (3 of 5 cycles failed that day);
the failure rate has gotten worse, not better, and (per a quick check of later logs) persists into 2026-09-13.
Only the one `aggressive-21Z` call — the first decision cycle to run after the profile split, ahead of the other
two — got a live response; both `baseline-21Z` and `conservative-21Z`, running minutes later at the same slot,
failed. This review does not investigate root cause (client timeout config vs. NIM-side latency vs. the 3x
increase in concurrent Kimi calls from the profile split); noted as observed.

**Missing run: no `risk-` check exists for the `21Z` hourly slot, on any track.** The last single-track risk
check is `risk-2026-09-12T20Z` (20:00:46Z); the next risk checks are the post-split
`risk-{aggressive,baseline,conservative}-22Z` (22:00–22:10Z). The profile-split commit landed at 20:33:29Z and the
first post-split decision cycles ran at 21:19–21:20Z, so the entire `21:00–21:59Z` hour had zero hourly
risk-check coverage on any track — a ~1h gap in the intended ≤1h stop-loss/drawdown/daily-loss safety net. No
adverse event occurred during that hour (confirmed via the 22Z checks and the 21Z decision cycles' own
RiskGateway passes, all clean), but the gap itself is a real cadence miss, not an accepted zero-slot (unlike a
`00Z` gap on an experiment's first day).

No other gaps: `experiment-` slots 00/04/08/12/16/20Z all present for the single track; `risk-` slots 00Z–20Z all
21 consecutive slots present. `policy_hash` is internally consistent per profile throughout (baseline
`8dcd8044...`, aggressive `e38cadc1...`, conservative `b3cd4a38...` — three different hashes are expected, since
each profile merges a different `config/policies/<profile>.json` over `DEFAULT_POLICY`).

## Anomalies requiring manual inspection

- **⚠ Repeated/worsening Kimi (NVIDIA NIM) failures.** 8 of 9 decision cycles failed today (7 timeouts, 1 rate
  limit), up from 3 of 5 on 2026-09-11. Only one live Kimi decision all day. If this persists it means the
  experiment is effectively running on RiskGateway-only fallback HOLDs almost all the time, not on live Kimi
  proposals. Flagged previously; still unresolved. Worth Stanley's attention.
- **⚠ One-hour gap in hourly risk-check coverage (`21Z`, all tracks)**, coinciding with the profile-split
  deployment. No stop/drawdown/loss exit was missed (nothing breached during or around that hour), but the safety
  cadence itself was not met for that slot.
- **⚠ Multi-profile split not recorded in `CHANGELOG.md` or reflected in the experiment version.** Per
  `CHANGELOG.md`'s own stated policy ("Any change that affects strategy conditions must be recorded here **and**
  in `state/experiment.json` (`changelog[]`), with the experiment version bumped"), adding a 5x-leverage
  `aggressive` profile and a halved-risk `conservative` profile is unambiguously a strategy-condition change. Yet:
  - `CHANGELOG.md` (repo root) has no entry after `v1.1` (2026-09-11T02:33:47Z) — the multi-profile change is
    undocumented there.
  - All three profiles' `state/<profile>/experiment.json.experiment_version` still reads `v1.1`, and each
    `changelog[]` array only contains the original v1.1 baseline entry (aggressive/conservative's copies are
    dated 2026-09-12T20:33:29Z but describe themselves as "baseline: 5-symbol spot universe... fixed POLICY" —
    the same boilerplate text as the original baseline entry, not a description of the leverage/half-risk change).
  - Root `config/experiment.json` also still describes a single-track `v1.1` experiment with no mention of
    profiles.
  This looks like a documentation/bookkeeping gap around an otherwise clearly-intentional, human-authored change
  (commit `0a687ea`, authored by Stanley) rather than a corrupted or unauthorized change — `universe` (5 symbols)
  and nominal `cadence` (`every_4h` per profile) are unchanged, and each profile's own `policy_hash` is internally
  consistent across all of its records. Flagging for Stanley's awareness per this review's integrity-check scope,
  not as a suspected compromise.
