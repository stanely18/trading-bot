# Daily Review — 2026-09-11

**Status: read-only review. No strategy, prompt, risk-parameter, or code changes were made or are recommended here.**

## Scope note — first (partial) day of v1.1

The current `v1.1` experiment started at **2026-09-11T02:33:47Z** (`state/experiment.json.started_ms`,
confirmed by `CHANGELOG.md`). So this UTC day is a **~21.5h partial window**
(02:33:47Z → 24:00:00Z), not a full 24h — there is no `experiment-2026-09-11T00Z`
cycle and no `risk-2026-09-11T00Z`/`01Z` check because the experiment had not
started yet at those slot times. This is expected, not a missing-run gap.

Records reviewed: 5 `experiment-` cycles (04Z, 08Z, 12Z, 16Z, 20Z — all slots
from experiment start onward, none missing), 22 `risk-` checks (02Z through
23Z, hourly, consecutive, none missing), 5 `logs/workflow/experiment-*` +
22 `logs/workflow/risk-*` entries, 3 `logs/errors/experiment-*` entries, and
`trades/trades.csv`.

## Daily PnL

- **NAV at window start** (experiment init, 02:33:47Z): **10.0000 USDT** (100% cash, by construction).
- **NAV at window end** (last record, `risk-2026-09-11T23Z`, 23:00:02Z): **9.9974 USDT**.
- **Realized PnL:** −0.0010 USDT (one taker fee on the single fill).
- **Unrealized PnL (end of window):** −0.0016 USDT (small mark-to-market loss on the open BTC-USDT position).
- **Total PnL (end of window):** −0.0026 USDT (**−0.026%** of starting NAV).
- Intraday NAV ranged from a low of 9.9955 (11Z) to a high of 10.0249 (14Z,
  also the day's peak equity) — a max intraday drawdown from that peak of
  ~0.28%, far under the 10% `max_drawdown` halt threshold. No day came close
  to the 2% `daily_loss` threshold either (worst same-day-anchor loss was
  ~0.04%).

## Benchmark comparison

`state/experiment.json.benchmark_vs_strategy` only ever holds the *latest*
snapshot (currently reflecting `experiment-2026-09-12T12Z`, outside this
review's window), so it cannot be quoted directly for 2026-09-11. Instead,
this reconstructs the 2026-09-11-window values from the static benchmark
unit holdings (`state/experiment.json.benchmarks`, unchanged since experiment
init) applied to the market-price snapshot in `risk-2026-09-11T23Z.json`
(the last record of the window):

| Series | NAV @ start | NAV @ 23Z | Change |
|---|---|---|---|
| **Strategy (Kimi + RiskGateway)** | 10.0000 | 9.9974 | **−0.026%** |
| btc_buy_hold | 10.0000 | 10.0328 | +0.328% |
| equal_weight | 10.0000 | 10.1538 | +1.538% |
| momentum_baseline | 10.0000 | 10.0575 | +0.575% |

The strategy underperformed all three benchmarks on this first day — it held
~90% cash for essentially the whole window (one 10%-allocation BTC buy at
08Z) while the broader 5-symbol universe drifted upward, so the benchmarks
(fully invested from t0) captured more of that move. One partial day is not
evidence of anything; noted as observed, per scope.

## Trades summary

**1 fill** for the day, from `trades/trades.csv`:

| run_id | time (UTC) | symbol | side | qty | price | fee |
|---|---|---|---|---|---|---|
| experiment-2026-09-11T08Z | 08:00:05.784Z | BTC-USDT | BUY | 0.000012941848448662664 | 77268.71505 | 0.001 |

Notional ≈ 1.0000 USDT (exactly the 10% `max_order_fraction` of the 10 USDT
starting NAV). No sells, no other symbols traded.

## Notable Kimi decisions

- **04Z** — `market_regime: risk_off`, `portfolio_view: neutral`, **zero
  candidates** → HOLD (adapter/risk gateway both held, no error).
- **08Z** — `market_regime: neutral`, `portfolio_view: neutral`. Two
  candidates:
  - BTC-USDT **BUY**, confidence 0.45, target allocation 10% — thesis: price
    reclaimed SMA20 (77230 > 77118) with positive 10-day momentum (+0.52%)
    and neutral RSI; "small starter long in a flat market". Adapter sized
    this to the full 10% cap and RiskGateway approved/filled it as-is (no
    resize needed).
  - ETH-USDT **HOLD**, confidence 0.40, target allocation 0% — thesis:
    strongest 10-day momentum but highest 20-day volatility in the universe
    and still below SMA50; explicitly deferred for confirmation. Not acted
    on (correctly, since action=HOLD).
- **12Z, 16Z, 20Z** — no Kimi output at all; see Technical failures below.
  Adapter/risk gateway correctly defaulted to HOLD in all three.

## Confidence calibration

Only one candidate was ever actioned (BTC-USDT BUY, confidence 0.45); the
sample is far too small (n=1) for any calibration read. Directionally: BTC
went from an entry of 77268.71505 to 77147.40 by day's end (~−0.16%),
consistent with the small unrealized loss above. The one HOLD candidate
(ETH-USDT, confidence 0.40) was, by definition, not tested against an
outcome. No conclusion drawn.

## Risk-engine interventions

**None.** Across all 5 decision cycles' `adapter`/`risk_result` and all 22
`risk-` checks' `risk_status`/`risk_exits`:
- 0 resized proposals (the one BUY was sized exactly at the 10%
  `max_order_fraction` cap and required no clamping).
- 0 rejected proposals.
- 0 stop-loss / drawdown / daily-loss exits in `logs/risk/`.
- `halted: false` throughout state, portfolio, and every risk-check record.

## Possible overtrading

None. 1 order for the day against a `max_daily_orders` cap of 24 (well under
even a single 4h cycle's headroom), no repeat activity in the same symbol
after the 08Z buy, no cycles anywhere near the daily cap.

## Contradictory theses

None. BTC-USDT was bought once and never sold same-day; ETH-USDT's only
candidate was a HOLD; no other symbol had any candidate. No opposing theses
across adjacent cycles (the three timed-out cycles produced no thesis at
all).

## Technical failures

**3 of 5 experiment cycles (60%) failed on the model call**, all with the
identical error `model:model call failed: TimeoutError: The read operation
timed out` (Kimi K3 via NVIDIA NIM, `moonshotai/kimi-k3`):

- `experiment-2026-09-11T12Z`
- `experiment-2026-09-11T16Z`
- `experiment-2026-09-11T20Z`

That's **three consecutive** 4h slots (12Z→20Z, 12 hours straight) with no
live model decision. In each case:
- `logs/errors/experiment-2026-09-11T{12,16,20}Z.json` records the same
  timeout error.
- `logs/workflow/...` shows step `model: "error_hold_fallback"` but overall
  `result: "ok"` — the cycle still ran to completion and recorded a clean
  HOLD via RiskGateway; no crash, no missed/skipped run, no state corruption.
- No fills, no errors elsewhere, `halted` stayed `false`.

So the fail-safe (timeout → forced HOLD, RiskGateway still runs) worked
exactly as designed with zero operational impact — but only 2 of 5 scheduled
4h cycles (04Z, 08Z) actually got a live Kimi decision on this first day.

No other technical issues: no gaps in the `experiment-` slot sequence
(04/08/12/16/20Z, all present — 00Z correctly absent, pre-dating experiment
start), no gaps in the `risk-` hourly sequence (02Z–23Z, all 22 consecutive
slots present), no workflow-health anomalies beyond the three model timeouts
above, `policy_hash` (`8dcd8044...`) constant across every 09-11 record.

## Anomalies requiring manual inspection

- **⚠ Repeated Kimi/NVIDIA NIM timeouts.** 3 of 5 trading cycles (60%) on the
  very first day of the live v1.1 run failed to get any model response
  (`TimeoutError: The read operation timed out`), including three in a row
  (12Z, 16Z, 20Z). The RiskGateway fallback handled it safely each time, but
  if this failure rate persists it effectively cuts the intended "6
  decisions/day" cadence down to 1–2 live decisions/day. Worth Stanley's
  attention — nothing in the available logs indicates root cause (network,
  NIM-side, or client timeout config), this review does not investigate
  further per its read-only/no-code-changes scope.
- **No experiment-integrity concerns found.** `policy_hash`, `universe`
  (5 symbols), and `cadence` (`every_4h`) are consistent across every
  2026-09-11 record and match `state/experiment.json` and `CHANGELOG.md`'s
  v1.1 entry (live start 2026-09-11T02:33:47Z, `initial_cash` 10 USDT). No
  halts, no re-inits, no hash changes observed on this day.
