# 只供人工設定；程式不會自動載入 .env，也不會要求真實交易金鑰。
# 雲端執行請改用 GitHub Actions Secrets / Variables，不要把值寫進 .env、prompt 或 commit。

# --- 交易 agent（NVIDIA NIM 上的 Kimi K3；未設定則 cycle 只做 HOLD）---
# 於 build.nvidia.com 自行註冊取得。GitHub Secret 名稱相同：NVIDIA_API_KEY。
NVIDIA_API_KEY=
# cycle 使用的模型：none（預設，只做 HOLD 基礎設施檢查）或 nvidia
TRADING_MODEL=none

# --- OKX（第一階段：僅 Demo 唯讀探針 / 未來 Demo 帳戶讀取）---
# 新舊命名皆可讀，優先 OKX_*；OKX_DEMO_* 為相容別名。
OKX_API_KEY=
OKX_SECRET_KEY=
OKX_PASSPHRASE=
# 雙重開關才會真的對 OKX Demo 下單（仍帶 x-simulated-trading:1，永不接真錢）：
#   1) 這個環境變數設為 1  2) 程式端 OKXDemoBroker(allow_orders=True)
DEMO_ORDER_EXECUTION_ENABLED=0

# --- 可選遠端 HTTP gateway（既有 POC transport，非雲端 runtime 必需）---
TRADING_GATEWAY_TOKEN=
TRADING_DB=state/experiment.sqlite3
TRADING_PORT=8765
